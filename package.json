{
  "name": "tsbindgen",
  "version": "1.0.0",
  "description": "Generate TypeScript declarations from .NET assemblies",
  "type": "module",
  "private": true,
  "scripts": {
    "validate": "cd .tests && npm install && cd .. && node scripts/validate.js"
  }
}
